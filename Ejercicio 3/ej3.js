let num=parseInt(window.prompt("Escribe un número"))
let num1=parseInt(window.prompt("Escribe un número"))

function suma(numero1, numero2) {
  resultado = numero1 + numero2;
  return suma
}

console.log(suma(num, num1))